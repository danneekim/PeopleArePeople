const { db, pgp } = require('../config/conn');

// This brings in our "project3_db" database
// Where we can refer to as "db"
// In order to execute sql query functions

module.exports = {

    // This is where we would write our sql query functions.
    
    // EX function:
    // index(){
    //     return db.all(
    //         `SELECT * 
    //         FROM TABLENAME1`
    //     );
    // },
}