const TABLE1Model = require('../models/TABLE1model')

// This is where would bring in our SQL Model functions
// AND assign them to approperiate controller functions
// IN ORDER to store respective data in res.locals
// AND to export them out to our router.



module.exports = {
    // This is where would write our functions that would store the data in our res.locals
    // EX function:

    // getAll(req,res,next){
    //     TABLE1Model.index()
    //     .then( data => {
    //         res.local.errthang = data;
    //         next();
    //     })
    //     .catch(next);
    // }
    
}